dprint-js is a javascript port so you can run it without downloading dprint binary
and plugins from external sources. Instead you can use node/bun/deno to manage
them and use your favorite js runtime to format your files.

Formatting is handled by https://github.com/dprint/js-formatter

The config is read from dprint.json 
plugins can refer to npm packages as found in package.json deps.



