// Poorly formatted TypeScript file for testing

function hello(name: string) {
  console.log("Hello, " + name);
}

const obj = { a: 1, b: 2, c: 3 };

interface Person {
  name: string;
  age: number;
}

export { hello, obj };
